{
  "name": "project-management-system",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@11.8.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  }
}
